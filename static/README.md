# /static/

- `assets` specifically refers to the contents of the loading page. 
- `page_assets` and `templates` excluding `templates/pow_shield` are whatever you might use in the website that the system protects (all of which is now deprecated unless you are somehow hosting your website inside this thing as a sub-application, which that is itself deprecated.)
- `templates/pow_shield` only contains the loading screen.